@page "/"
@using Lyster.App.Components.Shared

<div class="min-h-screen bg-gradient-to-br from-bg-900 via-bg-850 to-bg-800 flex items-center justify-center p-6">
    <!-- Animated wave background -->
    <div class="absolute inset-0 opacity-10">
        <div class="wave wave1"></div>
        <div class="wave wave2"></div>
        <div class="wave wave3"></div>
    </div>

    <!-- Main content -->
    <div class="relative z-10 w-full max-w-2xl text-center space-y-12">
        <!-- Logo/Brand -->
        <div class="space-y-4">
            <div class="flex justify-center">
                <svg class="w-20 h-20 text-accent" viewBox="0 0 40 40" fill="currentColor">
                    <path d="M20 0C8.954 0 0 8.954 0 20s8.954 20 20 20 20-8.954 20-20S31.046 0 20 0zm0 36c-8.822 0-16-7.178-16-16S11.178 4 20 4s16 7.178 16 16-7.178 16-16 16z"/>
                    <path d="M28 20c0 4.418-3.582 8-8 8s-8-3.582-8-8 3.582-8 8-8 8 3.582 8 8z"/>
                    <circle cx="20" cy="20" r="4"/>
                </svg>
            </div>
            <h1 class="text-6xl font-bold text-text-primary">
                <span class="gradient-text">TIDAL</span>
            </h1>
            <p class="text-xl text-text-secondary">
                Dive into a world of high-fidelity sound
            </p>
        </div>

        <!-- Placeholder Image -->
        <div class="relative">
            <div class="aspect-video rounded-2xl overflow-hidden shadow-2xl bg-gradient-to-br from-accent/10 via-accent-2/10 to-bg-800 border border-border-muted">
                <!-- Placeholder image - you can replace with actual image -->
                <div class="w-full h-full flex items-center justify-center">
                    <svg class="w-32 h-32 text-text-tertiary opacity-50" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 19V6l12-3v13M9 19c0 1.105-1.343 2-3 2s-3-.895-3-2 1.343-2 3-2 3 .895 3 2zm12-3c0 1.105-1.343 2-3 2s-3-.895-3-2 1.343-2 3-2 3 .895 3 2zM9 10l12-3"/>
                    </svg>
                </div>

                <!-- Overlay with gradient -->
                <div class="absolute inset-0 bg-gradient-to-t from-bg-900/60 to-transparent"></div>
            </div>

            <!-- Floating stats/features -->
            <div class="absolute -bottom-6 left-1/2 transform -translate-x-1/2 flex gap-8">
                <div class="glass-surface px-6 py-3 rounded-full">
                    <p class="text-sm text-text-secondary">
                        <span class="font-bold text-accent">100M+</span> Songs
                    </p>
                </div>
                <div class="glass-surface px-6 py-3 rounded-full">
                    <p class="text-sm text-text-secondary">
                        <span class="font-bold text-accent">Hi-Fi</span> Quality
                    </p>
                </div>
            </div>
        </div>

        <!-- CTA Buttons -->
        <div class="pt-8 space-y-4">
            <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
                <a href="/login" class="w-full sm:w-auto">
                    <PrimaryButton FullWidth="true" Size="lg">
                        <span class="px-8">Login</span>
                    </PrimaryButton>
                </a>

                <a href="/register" class="w-full sm:w-auto">
                    <GhostButton Variant="bordered" FullWidth="true" Size="lg">
                        <span class="px-8">Register</span>
                    </GhostButton>
                </a>
            </div>

            <p class="text-sm text-text-tertiary">
                Start your free trial today. No credit card required.
            </p>
        </div>

        <!-- Features -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 pt-8">
            <div class="space-y-2">
                <div class="w-12 h-12 rounded-full bg-accent/20 flex items-center justify-center mx-auto">
                    <svg class="w-6 h-6 text-accent" fill="currentColor" viewBox="0 0 20 20">
                        <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
                    </svg>
                </div>
                <h3 class="font-semibold text-text-primary">Lossless Audio</h3>
                <p class="text-sm text-text-tertiary">Experience music in studio quality</p>
            </div>

            <div class="space-y-2">
                <div class="w-12 h-12 rounded-full bg-accent/20 flex items-center justify-center mx-auto">
                    <svg class="w-6 h-6 text-accent" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M3 4a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm0 4a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm0 4a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm0 4a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1z"/>
                    </svg>
                </div>
                <h3 class="font-semibold text-text-primary">Curated Playlists</h3>
                <p class="text-sm text-text-tertiary">Discover handpicked collections</p>
            </div>

            <div class="space-y-2">
                <div class="w-12 h-12 rounded-full bg-accent/20 flex items-center justify-center mx-auto">
                    <svg class="w-6 h-6 text-accent" fill="currentColor" viewBox="0 0 20 20">
                        <path d="M2 11a1 1 0 011-1h2a1 1 0 011 1v5a1 1 0 01-1 1H3a1 1 0 01-1-1v-5zM8 7a1 1 0 011-1h2a1 1 0 011 1v9a1 1 0 01-1 1H9a1 1 0 01-1-1V7zM14 4a1 1 0 011-1h2a1 1 0 011 1v12a1 1 0 01-1 1h-2a1 1 0 01-1-1V4z"/>
                    </svg>
                </div>
                <h3 class="font-semibold text-text-primary">Personalized</h3>
                <p class="text-sm text-text-tertiary">Recommendations just for you</p>
            </div>
        </div>
    </div>
</div>

<style>
    /* Gradient text effect */
    .gradient-text {
        background: linear-gradient(135deg, var(--accent) 0%, var(--accent-2) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    /* Wave animations */
    .wave {
        position: absolute;
        bottom: 0;
        left: 0;
        width: 200%;
        height: 100%;
        background: linear-gradient(180deg, transparent, rgba(0, 212, 255, 0.03));
        animation: wave 15s linear infinite;
    }

    .wave::before,
    .wave::after {
        content: '';
        position: absolute;
        width: 200%;
        height: 100%;
        top: 0;
        left: 50%;
        transform: translate(-50%, -75%);
    }

    .wave::before {
        border-radius: 45%;
        background: rgba(0, 212, 255, 0.05);
        animation: wave 15s infinite linear;
    }

    .wave::after {
        border-radius: 40%;
        background: rgba(139, 92, 246, 0.03);
        animation: wave 20s infinite linear;
    }

    .wave1 {
        animation-delay: 0s;
        opacity: 0.3;
    }

    .wave2 {
        animation-delay: -5s;
        opacity: 0.2;
    }

    .wave3 {
        animation-delay: -10s;
        opacity: 0.1;
    }

    @@keyframes wave {
        0% {
            transform: translateX(0) translateY(0) rotate(0deg);
        }
        100% {
            transform: translateX(-50%) translateY(10%) rotate(360deg);
        }
    }
</style>

@code {

}
